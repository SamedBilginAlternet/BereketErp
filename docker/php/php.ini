[PHP]
; Raise memory limit for Laravel bootstrap
memory_limit = 256M

; Keep default upload size reasonable
upload_max_filesize = 20M
post_max_size = 20M

; Avoid slow DNS lookups for outbound connections
; (no effect on most requests but good to have)
default_socket_timeout = 10

[Date]
date.timezone = Europe/Istanbul
