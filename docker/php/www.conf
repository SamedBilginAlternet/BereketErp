[www]
user = bereket
group = bereket

listen = 9000

; Static pool — predictable, no spawn overhead per request
pm = static
pm.max_children = 20

; Recycle workers after 500 requests to prevent memory leaks
pm.max_requests = 500
